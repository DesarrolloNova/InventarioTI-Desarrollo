@model IEnumerable<InventarioTI.Models.UsuarioAsignacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAsignacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaInicioAsignacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFinAsignacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Asignado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAsignacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicioAsignacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinAsignacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEmpleado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEmpleado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEmpleado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
